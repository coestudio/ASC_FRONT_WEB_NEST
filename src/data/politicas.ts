// STUB — dados reais de políticas ainda não migrados do site anterior.
// TODO: preencher com o conteúdo real e remover este aviso.
export type Politica = {
  slug: string;
  nome: string;
  conteudo?: string;
};

export const politicas: Politica[] = [];
