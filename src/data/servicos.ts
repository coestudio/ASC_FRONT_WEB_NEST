// STUB — dados reais de serviços ainda não migrados do site anterior.
// TODO: preencher com o conteúdo real e remover este aviso.
export type Servico = {
  slug: string;
  nome: string;
  conteudo?: string;
};

export const servicos: Servico[] = [];
