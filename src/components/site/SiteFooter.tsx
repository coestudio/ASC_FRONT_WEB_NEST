// STUB — footer real do site ainda não migrado.
export function SiteFooter() {
  return (
    <footer className="border-top bg-light py-4 mt-5">
      <div className="container text-muted small">
        © {new Date().getFullYear()} Alex Stewart Agriculture
      </div>
    </footer>
  );
}
